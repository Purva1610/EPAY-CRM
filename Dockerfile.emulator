# Firebase Emulator Dockerfile
# Builds a container with Firebase CLI tools pre-installed
FROM node:26-alpine

# Install Firebase CLI globally
RUN npm install -g firebase-tools@latest

# Create working directory
WORKDIR /app

# Expose emulator ports
EXPOSE 4000 8080 9000 9099

# Default command
CMD ["firebase", "--version"]
